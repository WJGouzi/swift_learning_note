//: Playground - noun: a place where people can play


// 1.swift如何导入框架
import UIKit

// 2.定义标识符
// swift定义的标识符，告诉编译器，这个标识符是常量还是常量
// 声明的格式： let/var 标识符的名称 ：标识符的数据类型 = 值 (一般数据类型写在标识符的后面)
// 声明常量
let i : Int = 10
// 声明变量
var a : Double = 20.01
a = 30.02

// 3.语句结束
// 如果一行中只有一条语句，可以不用加`;`，可以省略
// 如果一行中有多条语句，必须加，但是这种写法不好。

// 4.打印
print(i)
print("a is \(a)" )